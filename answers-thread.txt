两个线程同时向同一个 bucket 插入时会丢失 key。比如线程 A 和线程 B 都先读到
同一个链表头 old，A 把新结点 a 的 next 设为 old，B 也把新结点 b 的 next 设为
old。随后 A 把 bucket 头写成 a，B 又把 bucket 头写成 b，A 插入的结点就从链表中
消失了。单线程时这几步不会交错，所以不会出现这个问题。给每个 bucket 单独加锁
后，同一条链表上的插入会串行执行，不同 bucket 上的操作仍然可以并行。
